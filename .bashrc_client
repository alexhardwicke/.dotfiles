if [ ! "$TMUX" == "" ]; then
    source ~/.dotfiles/.bashrc_session
else
    source ~/.dotfiles/.bashrc_base

    echo -ne "\e]0;Bash\a"

    if [ "$(pgrep tmux)" == "" ]; then
        exec tmux
    else
        tmux attach &> /dev/null
    fi
fi
